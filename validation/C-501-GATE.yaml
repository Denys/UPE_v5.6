schema_version: '1.0'
gate_id: C-501-CODEX-APP-SERVER-ADAPTER
generated_at: '2026-07-22T03:30:00+02:00'
status: PASS
status_reason: >-
  The schema-pinned synchronous adapter confines raw Codex App Server JSON-RPC
  to its boundary and passes 340 repository tests plus strict Windows static,
  reference, schema and report gates without starting a real provider process.

repository:
  identity: Denys/UPE_v5.6
  base_commit: 356b5b2ed53289bc6752c466d2cd8a436abe0bc4
  functional_head: ec04ed06498dfce466e062b9d0f7e2577211a6f3
  pull_request: https://github.com/Denys/UPE_v5.6/pull/16

criteria:
  - id: C-501-SCHEMA-COMPATIBILITY
    verdict: PASS
    evidence:
      - initialize/initialized and thread/start, thread/resume, turn/start and turn/interrupt match the accepted generated stable schema.
      - Exact Codex CLI 0.144.3 and schema-reference pins fail closed; no invented compatibility RPC or live compatibility claim remains.
      - Generated initialize capabilities, text input fields, response wrappers, turn statuses and approval decisions have adversarial regressions.
  - id: C-501-TRANSPORT
    verdict: PASS
    evidence:
      - JSON-RPC responses, notifications and server requests are parsed as distinct strict envelopes.
      - Interleaved messages are preserved; malformed JSON/envelopes, boolean IDs, RPC errors, missing executable, stdout closure and process exit normalize fail closed.
      - Provider stderr length may be reported but its contents are never exposed.
  - id: C-501-SESSION-TURN-LIFECYCLE
    verdict: PASS
    evidence:
      - Startup, shutdown and initialize preflight are idempotent and reset stale local handles after stop.
      - Thread create/resume and turn submit preserve Run, Task, request, provider thread and provider turn identity.
      - Duplicate requests, provider identity collisions and forged session/turn handles are refused.
  - id: C-501-STREAM-TERMINAL
    verdict: PASS
    evidence:
      - Real nested turn, item, delta, error and approval messages translate to provider-neutral events with contiguous local ordering.
      - Output deltas and provider payloads are redacted into bounded reference-only summaries.
      - Success, failure, interruption and host-requested cancellation emit one terminal and return without waiting for another message.
  - id: C-501-APPROVAL-INTERRUPT
    verdict: PASS
    evidence:
      - Approval allow/deny replies use the original server request ID and generated accept/decline values.
      - Turn interrupt uses the generated threadId/turnId request and session cancellation interrupts every active turn without inventing thread/cancel.
  - id: C-501-REGRESSION
    verdict: PASS
    evidence:
      - Nineteen focused C-501, 312 unit and 340 complete repository tests pass on Windows.
      - Lock, Ruff lint/format, strict mypy, work-specification, reference, schema, report freshness and diff checks pass.
      - Two Cloud review cycles were independently checked; wire-schema and runtime defects were reproduced and repaired locally before acceptance.
  - id: C-501-SCOPE-AND-LIMITS
    verdict: PASS
    evidence:
      - Functional code remains confined to the two canonical C-501 paths; coordinator evidence/state/report paths are serialized separately.
      - Unit tests use fake/in-memory transports and start no real Codex process, shell, model, network or external action.
      - C-502 live smoke, C-503 recovery, C-409 CLI wiring and C-505/C-506 security integration remain explicitly unclaimed.

commands:
  - uv lock --check
  - uv run pytest -q tests/unit/test_codex_adapter.py
  - uv run pytest -q tests/unit
  - uv run pytest -q
  - uv run ruff check .
  - uv run ruff format --check .
  - uv run mypy --strict src tests
  - uv run python scripts/validate_work_specifications.py
  - uv run python scripts/validate_references.py
  - uv run python scripts/validate_schema.py
  - uv run pytest -q tests/test_capability_readiness_report.py
  - uv run python scripts/update_capability_readiness_report.py --check
  - git diff --check

known_limits:
  - Static/generated-schema conformance is accepted; live App Server behavior remains C-502.
  - Restart/reconnect recovery, CLI integration and host security policy are later work packages.
  - Python 3.12 and 3.13 remain unverified.

next_action: Merge PR #16 and run post-merge Git, report freshness and rendered-report verification.

# Fixture repository boundary

This directory is the reserved, repository-contained location for the deterministic fixture
implemented by canonical task `C-305`.

`C-302` creates only this explicit boundary. It does not claim that the fixture initializes,
has baseline commands, or exercises harness behavior. Until `C-305` supplies and validates those
artifacts, this directory is documentation rather than test evidence.

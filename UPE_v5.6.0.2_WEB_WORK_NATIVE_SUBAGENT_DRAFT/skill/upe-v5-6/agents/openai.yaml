interface:
  display_name: "UPE v5.6"
  short_description: "Audit and build deployable prompt systems"
  default_prompt: "Use $upe-v5-6 to audit and improve this reusable framework. On Web Work, use the native-subagent adapter when its current-run activation gate passes."
policy:
  allow_implicit_invocation: true
